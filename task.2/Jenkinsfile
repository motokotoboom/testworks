pipeline{
    agent any
    stages {
        stage('Get source') {
            steps {
                echo 'getting source'
                checkout scm
                sh 'apt install docker.io'
            }
        }

    }
}

