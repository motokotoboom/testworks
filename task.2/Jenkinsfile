#!groovy
pipeline {
  agent none
  stages {
    stage('php test') {
      agent {
        docker {
          image 'php'
        }
        steps {
            checkout scm
            echo 'done'
        }
      }
  
    } 
  }
}